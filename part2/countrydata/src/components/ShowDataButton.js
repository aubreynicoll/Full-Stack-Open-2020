import React from 'react'

const ShowDataButton = ({onClick}) => {
  return (
    <button
      type="button"
      onClick={onClick}>
      show data
    </button>
  )
}

export default ShowDataButton
